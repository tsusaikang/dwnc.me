# 장기 작업 운영 규칙

이 규칙은 이 프로젝트의 모든 메인 세션과 하위 에이전트에 적용한다.

## 역할과 위임

- 메인 세션은 작업 순서와 범위를 조정하고, 하위 에이전트의 결과를 통합해 검토하며, 공식 상태를 갱신하고 사용자에게 보고한다. 주요 실무는 내용에 맞는 하위 에이전트에 위임한다.
- 하위 에이전트가 브라우저 탭·로그인 화면·데스크톱 앱·운영체제 클립보드·사용자 세션이나 메인 세션 전용 도구에 기능상 접근할 수 없으면, 메인 세션이 승인된 범위에서 그 동작만 최소한으로 직접 수행한 뒤 오케스트레이션으로 돌아간다. 접근 불가가 한 번 확인되면 형식적인 재위임을 반복하거나 사용자에게 대신 하도록 넘기지 않는다.
- 이 예외는 삭제·구매·공개 전환·권한 변경·외부 전송 등에 필요한 승인 범위를 넓히지 않는다. 메인 세션이 직접 수행했다면 그 이유·범위·결과·미해결만 공식 상태와 사용자 보고에 짧게 남긴다.
- 서로 독립적인 작업은 병렬로 위임한다. 같은 파일이나 같은 로컬·외부 상태를 다루는 작업은 충돌을 피하도록 직렬로 위임한다.
- 하위 에이전트는 장황한 작업 과정보다 **결정·근거·산출물·검증·미해결**만 압축해서 반환한다. 메인 세션은 이를 그대로 전달하지 않고 전체 요청과 완료조건에 맞춰 다시 확인하고 통합한다.

## 새 주요 단계와 컨텍스트 관리

- 새 주요 단계를 시작하기 전에 다음 질문을 반드시 확인한다.

  **결과 수신 → 통합 → 검증 → PROJECT_STATE.md 갱신 → 사용자 보고까지 현재 컨텍스트에서 완료 가능한가?**

- 답이 아니거나 불확실해 안전한 마무리가 위험하면 새 실무를 시작하지 않는다. 먼저 `PROJECT_STATE.md`를 최신 상태로 만들고, 사용자에게 정확히 `새 작업 권장`이라고 알린 뒤 같은 프로젝트를 바로 이어갈 수 있는 복사 가능한 인수인계를 제공한다.
- 대화 내용만 프로젝트의 공식 상태로 여기지 않는다. 중간에 새 요청이나 조건이 들어오면 현재 계획과 공식 상태에 반영한 뒤 다음 순서를 조정한다.

## 사용자 결과와 작업 범위 우선

- 사용자가 요청한 결과와 명시한 완료조건을 가장 먼저 충족한다.
- 승인된 작업에 대한 후속 지적·정정·보완 요구는 문장 형식보다 현재 작업의 맥락과 목적에 따라 해석한다. 지적이 타당하고 수정 방향이 명확하며 승인 범위 안이면 재승인을 기다리지 않고 실제 산출물에 반영하고 필요한 확인까지 마친다. 인정·설명·향후 약속만으로 완료 처리하지 않는다.
- 사실을 창작하거나 범위를 임의로 넓히지 않는다. 별도 승인이 필요한 미승인 외부 행위는 실행하지 않으며, 사용자가 설명·검토·문서화만 요청하거나 변경하지 말라고 명시하면 그 제한을 따른다. 실행이 막힌 경우에만 구체적인 장애와 필요한 사용자 판단을 밝힌다.
- 보조 작업은 완료조건이나 중대한 위험을 직접 해결할 때만 수행하고, 위험성과 되돌리기 어려운 정도에 비례한 최소 범위로 제한한다.
- 충분한 근거가 확보되면 반복 검증과 추가 관리체계 생성을 멈춘다. 보조 작업이 본 작업을 지연하거나 범위를 넓히면 즉시 중단하고 본 작업으로 돌아가며, 남은 위험만 짧게 보고한다.

## 공식 상태와 사용자 보고

- `PROJECT_STATE.md`에는 최소한 **목표·완료조건, 확정된 결정과 이유, 완료한 작업, 주요 산출물과 위치, 검증 결과, 미해결 문제, 다음 행동, 중요한 제약과 승인 경계**를 최신 상태로 유지한다.
- 기본 읽기 대상은 이 지침, `PROJECT_STATE.md`, `docs/REQUIREMENTS.md`다. 관련 작업에 필요한 상세 요구사항·기술 계약만 추가로 읽고, 이력·감사 자료는 특정 사실 확인이 필요할 때 해당 부분만 조회한다.
- `docs/REQUIREMENTS.md`는 현재 유효한 미완료 요구사항만 둔다. 완료 항목은 안정 ID·완료조건·근거를 보존해 `docs/REQUIREMENTS_ARCHIVE.md`로 옮기며 최근 완료 목록도 누적하지 않는다. 완료된 별도 상세 요구사항은 보관 영역으로 이동하고 관련 링크를 고친다.
- 현재 상태에는 최신 판단에 필요한 운영 결과와 미해결 사항만 남긴다. 종료된 과정·인증·배포·검증 이력은 `docs/history/`에서 보존하고 현재판에는 짧은 링크만 둔다. 기술 계약은 현재 규칙, 요구사항은 완료조건, 상태는 다음 행동을 정본으로 삼아 긴 내용을 중복 기록하지 않는다.
- 사용자에게는 비개발자도 한 번에 이해할 수 있는 쉬운 한국어로 결과, 사용자에게 미치는 의미, 다음 행동을 먼저 설명한다. 기술 용어와 긴 식별값은 꼭 필요할 때만 쉬운 뜻과 함께 뒤에 덧붙인다.
