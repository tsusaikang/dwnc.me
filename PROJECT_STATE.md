# dwnc.me 프로젝트 공식 상태

최종 갱신: 2026-09-14 KST · 현재 목표·운영 기준선·미해결·다음 행동의 정본이다. 과거 기록은 필요할 때만 읽는다.

## 현재 목표와 완료 조건

- **CORE-019 진행 중:** 제목 글꼴을 설명하고 일반 본문 기본 글꼴만 제목 계열로 바꿔본다. 명시적 부분 글꼴·이탤릭·굵게·코드 서식과 원본/본문/작업본을 보존한다. 사용자 예시는 `/posts/587`이며 관련 확인 후 CORE-018 보완을 포함한 최종 Git 배포·운영 확인까지 진행한다.

- **CORE-018 후속 보완 진행 중:** 이전/다음 글 카드도 같은 표시 카테고리 안의 시간순 이웃으로 제한한다. 두 일상 분류의 표시 통합과 목록의 날짜·순번 정렬을 공유하며 왼쪽 다음·오른쪽 이전 배치를 유지한다. 구현·관련 시험은 하위 에이전트, 브라우저·통합·공식 상태·Git 자동 배포 확인은 메인이 맡는다. 기존 콘텐츠·작업본·주소·원본 분류는 변경하지 않는다. 최초 Git은 clean main `bef2cae`이며 최종 소스 배포와 운영 확인이 완료조건이다.

- **`DWNC-CORE-018` 완료:** 같은 카테고리 전체 글을 최신순 5개씩, 번호 최대 7개와 «/» 묶음 이동으로 탐색한다. 최초 현재 글 포함 페이지와 현재 글 표시를 제공하며 목록 이동은 본문 재요청·추가 조회수를 만들지 않는다. 시간순 카드도 왼쪽 다음 글·오른쪽 이전 글로 맞췄다. 최종 소스의 양쪽 Git 자동 배포와 운영 첫 글·중간 글 확인을 마쳤다. 구현은 하위 에이전트, 브라우저·통합·공식 기록은 메인이 맡았다. [완료 근거와 검증 한계](docs/history/카테고리_글_페이지_탐색_20260914_224127.md).

- **`DWNC-CORE-017` 완료:** 새 입력·붙여넣기 http(s)/www URL 자동 링크와 명시적 해제 유지 기능을 운영에 반영했다. 티스토리 실제 링크 선택 → 툴바 → 링크해제 흐름을 확인했다. 자체 작성 `/posts/597`의 빠진 Logitech 링크 한 곳도 공개 반영했다. 원래 링크가 없던 이전 596편은 수정하지 않았다. 최종 로컬 시험과 양쪽 Git 배포, 운영 편집기 최종 코드·목록 및 공개 글 링크를 확인했다. 운영 시험 글 입력은 자동 승인 검토가 차단해 실행하지 않았으며 미변경을 확인했다. [완료 근거·한계](docs/history/자동링크_구현_진행_20260913_201937.md), [직전 조사](docs/history/자동링크_및_이전글_조사_20260913_174723.md).

- **`DWNC-CORE-016` 완료:** ‘카테고리’ 용어 통일, 부모 행 전체 펼침/접기·꺾쇠·첫 ‘카테고리명 전체 보기’, 두 일상 분류의 표시 통합을 구현했다. 원본 분류·기존 글/작업본·기존 주소는 유지한다. 최종 소스의 공개/관리자 Git 자동 배포와 운영 카테고리·기존 일상 주소·관리자 필터 확인까지 마쳤다. [구현·검증 상세](docs/history/카테고리_탐색_개선_20260912_171354.md)에 로컬·운영 및 조회수 관측 한계를 구분했다.
- **`DWNC-CORE-015` 완료:** 관리자 좌측 상단 로고를 공개 블로그 홈의 새 탭 링크로 바꾸고 기존 외형·편집 화면과 키보드 접근을 유지했다. 최종 소스 Git 자동 배포와 운영 링크·클릭 확인을 마쳤다. 내장 브라우저에서 클릭에 따른 팝업 탭 생성은 확인하지 못했으며 아래 검증 범위를 따른다.
- **`DWNC-CORE-014` 완료:** 티스토리 실제 조작 관찰 → 사진 직접 이동·2/3장 묶기·그룹 분리/순서·인라인 정렬/크기 구현 → 로컬 시험 → 최종 소스 Git 자동 배포 → 운영 새 비공개 합성 글 1개의 저장·재열기 시험까지 마쳤다. 별도 배치 모달은 선택적 더보기이며 필수 조작을 본문에서 제공한다. [완료 상세](docs/history/이미지_직접조작_요구사항_20260911_183325.md)에 관찰·구현·검증·한계를 구분했다.
- **`DWNC-OPS-008` 완료:** Google 연결과 지정 계정의 실제 관리자 로그인을 확인했다. 2026-09-12 사용자가 Microsoft 로그인 추가를 취소해 해당 조건을 범위에서 제외하고 완료 원장으로 옮겼다. 기존 이메일 OTP와 24시간 세션은 유지한다. 실제 이메일과 연결 비밀값은 이 문서에 기록하지 않는다.
- **`DWNC-CORE-012` 착수 대기:** 별도로 등록한 결함 6건은 아직 고치지 않았다. 후속 착수 지시가 있어야 합성 재현 → 전체 보완 → 관련 회귀·내장 브라우저 → 최종 소스 Git 배포 → 운영 확인을 진행한다. 세부 완료 조건은 [결함 보완 요구사항](docs/결함_보완_요구사항_20260909_223512.md)을 따른다. F06은 소스 확인 상태이므로 응답 유실 합성 재현부터 필요하다.

| 하위 ID | 보완 대상 |
|---|---|
| F01 | 사진 삭제 후 실행 취소·다시 실행 |
| F02 | 공개 상태 전환 시 CSS 배경 전용 사진 참조 |
| F03 | 검색 통신 실패 안내·재시도 |
| F04 | 현재 검색어와 다른 늦은 결과·오류 무시 |
| F05 | 초안 생성 응답 유실 재시도의 중복 방지 |
| F06 | 사진 업로드 응답 유실·DB 기록 실패 재시도의 중복 방지 |

## 운영 기준선과 주요 산출물

- CORE-018 최종 기능 소스 `73c856b2608bfc0e610873c336cde683e3a31450`를 main에서 커밋·GitHub에 푸시했다. 마감 기록도 main에서 커밋·푸시한다. 현재 재개 기준은 원격 main이다.

- 공개 블로그 `https://dwnc.me`와 관리자 `https://admin.dwnc.me` 운영 중. 최종 기능 소스는 원격 main의 `73c856b`다. 양쪽 Git 자동 배포 Success와 Active deployment의 공개 `dc4e36e7`·관리자 `a335084f`를 확인했다. 카테고리 전체 글 페이지 탐색·시간순 카드 방향, 자동 링크·해제, 일상 표시 통합과 관리자 로고 링크가 운영에 반영됐다. Google 로그인은 운영 중이며 Microsoft 연결과 추가 이메일 운영 설정은 사용자 취소로 적용하지 않는다.
- 공개 홈 정상 표시와 인증된 관리자 내부 목록·편집기 진입을 확인했다. `DWNC-OPS-007`의 남은 관리자 확인도 완료돼 완료 원장으로 옮겼다. 과거 349편 공개·247편 비공개·총596편은 이전 기준선이며 현재 전체 글 수로 사용하지 않는다. 이번에는 전체 콘텐츠를 재집계하지 않았다.
- 승인된 신규 시험 글 **한 개**: `사진 직접 조작 비공개 합성 시험 20260911_190457`. 합성 문구·PNG 3장만 사용했다. 비공개 저장·재열기까지 완료하고 보존했다. 추가 시험이 필요해도 같은 글을 사용하며 공개·삭제하지 않는다. 기존 실제 글·작업본은 변경하지 않았다.
- 사진 직접조작: `src/lib/admin-image-direct.ts`, 연결: `src/lib/admin-ui.ts`. 기존 배치/표현/이력은 `src/lib/admin-image-layout.ts`, `src/lib/image-layout.ts`, `src/lib/admin-editor-history.ts`. 시험은 `scripts/test-image-direct.mjs`, `scripts/test-image-layout.mjs`, `scripts/test-editor-body-history.mjs`, 합성 브라우저 환경은 `scripts/serve-editor-fixture.mjs`다.
- **Git 동기화 원칙 정정(2026-09-12):** 다른 컴퓨터에서 작업을 이어갈 수 있도록 저장소 관리 대상인 코드·지침·요구사항·상태·이력·검사 변경을 작업 마감 전에 모두 커밋하고 가능하면 GitHub에 푸시한다. 이전 문서·검사 미커밋 변경도 이번에 함께 반영한다. 비밀값·비공개 원본·로컬 인증·빌드 산출물은 기존대로 제외한다. 문서만 푸시해도 현재 Cloudflare 연결에서 자동 빌드가 실행될 수 있으나 이번 동기화는 앱 기능·콘텐츠·운영 설정을 바꾸지 않는다.
- 기존 글·페이지·공지·서식, 작업본/공개본 분리, 예약·비공개·보호, 분류·설정·통계, 대표사진과 이미지/HTML 붙여넣기 기능은 유지한다. 완료가 모든 운영 상황을 지원한다는 뜻은 아니다.

## 확정된 결정과 검증 결과

- CORE-018: 기존 목록은 시간상 가까운 다른 글 5개였고 이전 2개/이후 3개 고정이 아니었다. 공통 렌더러로 전체 표시 카테고리를 최신순 탐색하며 기존 분류·본문·주소·작업본·접근 정책은 변경하지 않았다. 전체 editor:test와 최종 source-only 빌드, 정적 349편 및 Worker 회귀를 통과했다. 로컬 실제 390px와 키보드, 운영 묶음 이동·첫/중간 글 카드 방향을 확인했다. 운영 글은 preview=1로 읽었고 통계 재초기화는 하지 않았다. 운영 390px 적용은 브라우저 제약으로 확인하지 못했으며 상세는 완료 기록에 둔다.

- CORE-017 해제는 링크 안 커서 → 툴바 ‘링크’ → ‘링크 해제’다. 최종 editor:test·source-only build 통과(오류·경고 0, 기존 힌트 21), 로컬 입력·붙여넣기·해제·저장/재열기 확인을 마쳤다. 운영 최종 스크립트와 목록, 공개 홈·597 응답 및 보완 링크를 확인했다. 운영 실입력 시험은 승인 검토 차단으로 미실행이며 기존 합성 글도 그대로다. 공개 글 읽기는 preview=1·curl UA로 조회수 제외했고 통계 재초기화는 하지 않았다.

- CORE-016: 원본 TAXONOMY·CmsCategory는 그대로 두고 `src/lib/category-display.ts`의 표시 규칙으로 통합한다. 두 기존 카테고리 주소는 일상108편을 제공한다. 관리자 글 찾기·편집 선택은 일상 한 항목이며 기존 daily-stories 작업본을 저장하면 원래 ID를 유지한다. 원본 구조를 편집하는 카테고리 관리 화면은 기존 항목을 보존한다.
- CORE-016 최종 editor:test·신규 카테고리 회귀·source-only 빌드·Git 진입점 시험 통과. 내장 브라우저 로컬 합성 저장/재열기·키보드·390px 패널과 운영 부모 펼침/전체 보기·기존 주소·현재 위치·관리자 필터를 확인했다. 운영 실제390px 폭 확인은 브라우저 크기 적용 대상 문제로 주장하지 않으며 상세는 이력에 둔다.

- 로고 보완은 `src/lib/admin-ui.ts` 한 파일만 커밋·push했다. 기존 관리자 UI·공개/관리자 링크 시험, Cloudflare 설정 검사, 최종 source-only 빌드와 Git 진입점 시험이 통과했다. 타입 검사 오류·경고 0, 기존 힌트 21이다. 메인이 운영 관리자에서 `https://dwnc.me/`·`target="_blank"`·`rel="noopener noreferrer"`와 클릭 후 관리자 화면 유지를 확인했다. 내장 브라우저가 팝업 탭을 노출하지 않아 클릭에 따른 실제 새 탭 생성은 미확인이다. 링크 주소를 별도 새 탭으로 열어 공개 홈 정상 표시를 확인했으며 글·작업본은 변경하지 않았다.
- 티스토리에서 사진 옆으로 놓아 2/3장 묶기, 그룹 순서 변경, 사진 상단 가장자리로 단독 분리, 인라인 너비/정렬 적용을 실제 확인했다. 문단 영역 드래그는 성공하지 않았으며 우리 편집기의 문단 경계 이동은 별도로 로컬·운영에서 확인했다. 티스토리 새 합성 편집에 자동 저장 완료가 표시됐지만 명시적 임시저장 개수는 0이었고 임시저장·완료·발행 버튼은 누르지 않았다. 서버 자동저장 보관 범위는 미확인이다.
- 각 사진의 캡션·본문 서식을 보존한다. 원본 100%는 가용 폭 안으로 제한하고 문단 720px·전체 1200px은 각 원본 폭이 충분할 때만 허용한다. 크기 미확인 시 100%만 허용한다. 글자와 섞인 사진은 먼저 문단 밖으로 끌어낸 뒤 그룹·정렬·크기를 적용하도록 안내해 문단 서식 손상을 막는다.
- `editor:test` 전체, 요구사항 검사/시험, Git 배포 진입점 합성 시험과 최종 source-only 빌드 통과. 오류·경고 0, 빌드 내 미디어 사본 0. 기존 staging 타이머 시험의 첫 일시 실패는 단독/전체 재시험에서 통과했고 해당 시험을 수정하지 않았다.
- 로컬 PC/390×844에서 이동·2/3장 묶기·분리·순서·이동 손잡이, 개별 캡션·굵게/기울임·표 보존, 실행 취소/다시 실행·저장/재열기를 확인했다. 작은 원본 320px 제한과 큰 원본의 720px 표시, 모바일 미리보기 가로 넘침 없음을 확인했다. 실제 휴대폰 터치 실기는 아니다.
- 운영의 새 합성 글에서 2/3장 묶기·순서·문단 사이 분리 이동, 실행 취소 후 복원·다시 실행 정확 일치, C 캡션·문단 폭/좌측 정렬, 비공개 반영 후 재열기 HTML 정확 일치, 모바일 미리보기를 확인했다.
- 공개 홈에 시험 글 제목은 없다. 시험 글의 방문자 URL은 내장 브라우저 `ERR_BLOCKED_BY_CLIENT`로 열리지 않아 응답 코드·본문은 확인하지 못했다. 서버 404의 증거로 간주하지 않는다. 관리자 목록·저장 안내의 비공개 상태는 확인했다.

## 로그인 설정 조사

- 내장 대시보드에서 해당 도메인과 관리자 앱이 있는 계정을 확인하고, 화면의 계정 한 개가 프로젝트의 계정 확인값과 일치함을 대조했다. Google IdP를 등록하고 대상 관리자 앱에 추가했다. 공유 이메일 허용 정책과 다른 관리자 앱의 IdP 설정은 변경하지 않았다.
- `admin.dwnc.me`: Session Duration 24 hours, 허용 IdP는 One-time PIN과 Google, 두 방식 선택에 따라 instant authentication은 꺼짐, Cloudflare One Client 인증 끔.
- 관리자 이메일 한 개만 허용하는 정책은 두 관리자 앱이 공유한다. 정책 기간은 Same as application session duration, 전역 기간도 Same as application session timeout이다. 다른 앱에 영향을 주지 않으려면 이 앱의 기간만 바꾸면 된다.
- 계정의 등록 IdP는 Cloudflare·One-time PIN·Google이며 대상 앱은 PIN과 Google만 선택했다. Google IdP는 `892fbee1-2a9c-4892-aaaa-bee29009a8f9`다. Microsoft IdP는 없으며 사용자 취소에 따라 추가하지 않는다. 기존 공유 정책을 무분별하게 확장하지 않고 대상 관리자 앱과 지정 계정에 한정한다.
- **Google 완료:** [공식 안내](https://developers.cloudflare.com/cloudflare-one/integrations/identity-providers/google/)에 따라 개인 소유·조직 없음의 `dwnc-me-admin-login` 프로젝트를 사용했다. 사용자가 2026-09-12 데이터 정책 동의 체크와 Create를 직접 완료했다. Web 클라이언트 `dwnc.me Cloudflare Access`를 생성해 Access에 연결했으며 결제 연결·유료 서비스 활성화는 하지 않았다. 권한은 기본 신원 `openid profile email`만 사용한다. 비밀값은 일회 표시에서 연결 폼으로 직접 전달하고 파일·클립보드에 저장하지 않았다.
- 연결 시험에서 요청한 Google 계정과 `Your connection works!`를 확인했다. 기존 관리자 작업의 저장 완료를 확인한 뒤 Access에서 로그아웃하고 Google을 선택해 다시 로그인했다. 실제 관리자 글 목록과 활성화된 새 글 쓰기 버튼을 확인했으며 글을 생성·수정하지 않았다. 메인이 사용자 브라우저 세션 조작을 담당했고 구현은 앞서 하위 에이전트가 완료했다.
- **Microsoft 취소:** 2026-09-12 사용자가 Microsoft 로그인 추가를 취소했다. 사용자가 진행한 개인용 Azure 가입 2/3 주소 정보·약관 화면에서 주소 빈칸·약관 미동의·Next 비활성을 확인했고 가입은 완료되지 않았다. 메인이 가입 탭을 닫았다. Microsoft 앱·IdP·추가 이메일 secret은 생성하지 않았고 Automanix 설정도 변경하지 않았다. 가입·연결을 재개할 다음 행동은 없다.
- 운영 `src/lib/access-auth.ts`는 기존 단일 설정과 호환되는 `ACCESS_ADDITIONAL_ALLOWED_EMAILS` 선택적 운영 secret 설정을 지원한다. 쉼표 구분·정규화 후 정확 주소만 허용하며 기본 계정은 필수다. `scripts/test-native-editor.mjs`, `scripts/test-admin-session.mjs` 회귀와 Cloudflare 설정 검사·타입 검사 통과(오류·경고 0). 최종 source-only 빌드와 Git 진입점 시험 통과 후 인증 관련 3파일만 `d95264d`로 커밋·push했고 양쪽 배포·100% 활성화를 완료했다. 운영 추가 이메일 secret은 아직 없으므로 현재 실효 허용은 기존 기본 계정 한 개다. Google은 이 기존 계정과 일치해 추가 secret 없이 실제 로그인이 된다. 사용자 취소에 따라 추가 secret을 설정하지 않으며 이미 배포한 호환 코드는 되돌리지 않는다. 기존 글·작업본은 계정별로 분리되지 않아 데이터 변경은 필요 없다.
- Google 연결 입력: Access team origin은 `https://ancient-term-4cf0.cloudflareaccess.com`, callback은 같은 origin의 `/cdn-cgi/access/callback`이다. External/Web client로 이 origin과 callback을 등록했다.
- [공식 세션 문서](https://developers.cloudflare.com/cloudflare-one/access-controls/access-settings/session-management/)상 최대 1개월이지만 이번 요청은 로그인 방식 추가이며 기간은 바꾸지 않는다.

## 보존 경계와 후속 미해결

- **조회 통계 초기화 완료(2026-09-12 16:05 KST):** 사용자가 개발 확인 접속이 대부분이라고 판단해 기존 조회수 전체 초기화를 명시 승인했다. 하위 에이전트가 집계 전용 테이블과 영향 범위를 확인하고 메인이 일치 확인한 운영 D1의 내장 대시보드에서 `DELETE FROM post_view_daily;`를 한 번 실행했다. 실행 전 154개 일별 집계 행·총205회, 실행 후 행0·합계0이며 관리자 화면의 16:05:26 기준 오늘·최근30일·누적0을 확인했다. 글·작업본·미디어·설정은 변경하지 않았으며 코드 변경·재배포도 없다. 이후 조회는 기존 방식으로 다시 누적된다.
- **이번 확인의 조회수 관측:** 시작17:00:50에 오늘/30일/누적4, 종료17:18:21에15였다. 이번에는 집계 대상이 아닌 카테고리·관리자 화면만 탐색하고 운영 글 본문은 열지 않았다. 코드·합성 시험상 해당 경로는 집계하지 않지만 현재 통계는 접속 주체를 기록하지 않아 증가11회의 출처를 확인할 수 없다. 조회 정책 변경·통계 재초기화는 하지 않았다.
- **조회 제외의 남은 한계:** 공개 글을 일반 브라우저로 열어 확인한 개발 접속은 집계될 수 있다. `eligiblePostView`는 요청의 Access 헤더·쿠키 또는 관리자 Referer가 있을 때만 관리자 열람을 제외하며, 공개 화면의 별도 관리자 세션 확인과 집계 제외는 연결돼 있지 않다. 홈·목록·관리자 화면 자체는 집계 대상이 아니다. 이번 요청은 기존 숫자 초기화였으며 관리자·개발 열람 제외 방식은 변경하지 않았다.
- 원본 스냅샷·정규화 본문·공개 표현을 분리하고 원본·기존 서식·사진·작업본·주소를 보존한다. 이전기는 재실행하지 않는다. canonical `/posts/{globalSequence}`와 정확한 과거 alias를 유지하고 순번을 재정렬·재사용하지 않는다.
- 자동저장은 작업본만 바꾸며 공개/비공개/예약/보호 상태와 사진 소유·접근 정책을 유지한다. 기존 R2 key 덮어쓰기·삭제, raw 배포, DNS/route 변경, 비밀값·비공개 실제 콘텐츠 출력/기록은 금지한다. 현재 승인 범위는 CORE-018 카테고리 글 목록 페이지 탐색 구현·Git 자동 배포·운영 읽기 확인이다. 원래 미링크인 이전 글은 변경하지 않는다. 실제 글을 시험 목적으로 변경하거나 기존 시험 글을 공개·삭제하지 않는다.
- 댓글·방명록·구독·팀블로그 제외, 광고 후순위. 독립 사본·비공개 백업/복구·신규 이미지 최적화·공유/스크랩 10개 처리는 후속 선택이다. 실제 휴대폰·접근성 전체·대규모 부하·계정 상실·플랫폼 장애·비용/장애 알림은 미확인이다.
- 메인은 오케스트레이션·브라우저·통합·공식 상태, 하위 에이전트는 주요 실무를 맡는다. 모든 브라우저 작업은 내장 브라우저만 사용한다.

## 다음 행동과 문서 정본

- CORE-018의 같은 카테고리 이전/다음 글 보완을 구현·시험하고 Git 자동 배포·운영 확인 후 마감한다. CORE-012와 독립 사본·보관 정책은 별도 착수 지시나 선택을 기다린다.
- 로고 링크 보완은 운영 반영까지 마감했다. 로그인 추가 작업은 Google 완료·Microsoft 사용자 취소로 마감했으며 현재 추가 인증 작업은 없다. `CORE-012`는 별도 착수 지시를 기다린다.
- [AGENTS.md](AGENTS.md): 운영 지침. [현재 요구사항](docs/REQUIREMENTS.md): 미완료 완료조건. [완료 원장](docs/REQUIREMENTS_ARCHIVE.md): 안정 ID·이력. [미디어 계약](docs/MEDIA_SERVING_CONTRACT.md), [주소 계약](docs/URL_CONTRACT.md), [Git 배포 안내](docs/GIT_DELIVERY_20260910_142007.md): 기술 규칙.
- [이미지 직접조작 완료 상세](docs/history/이미지_직접조작_요구사항_20260911_183325.md): 관찰·구현·운영 결과. [직전 상태 이력](docs/history/PROJECT_STATE_HISTORY_20260912_003022.md): 이전 기준선과 종료 과정. [기존 상태 이력](docs/history/PROJECT_STATE_HISTORY_20260909_234401.md): 이전·개발·인증·배포의 과거 근거.
