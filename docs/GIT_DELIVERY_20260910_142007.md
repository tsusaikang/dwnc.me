# Git에서 Cloudflare로 배포하기

현재 상태: 로컬 실행 진입점·합성 시험·Git 파일셋 격리 소스 빌드를 확인했다. 공개·관리자 두 Worker에 같은 저장소의 main 브랜치 연결과 아래 명령을 저장했으며, 기존 빌드 토큰을 재사용하고 비운영 브랜치 자동 배포를 껐다. push·실제 CI 빌드·운영 반영은 아직 하지 않았다. 최신 실행 상태는 PROJECT_STATE.md를 따른다. 기존 서비스의 글·사진 저장 방식은 그대로 유지한다.

## 연결할 대상

기존 공개 Worker와 관리자 Worker를 각각 같은 저장소·같은 운영 브랜치에 연결한다. 두 Worker에 별도의 Workers Builds가 생기는 Cloudflare 공식 방식을 사용한다. 새 Pages 서비스나 DNS·route 변경은 필요 없다.

| 설정 | 공개 Worker | 관리자 Worker |
|---|---|---|
| Worker 이름 | `dwnc-me` | `dwnc-me-admin` |
| 저장소 루트 | `/` | `/` |
| 빌드 명령 | `npm ci && npm run cloudflare:git:test` | `npm ci && npm run cloudflare:git:test` |
| 배포 명령 | `npm run cloudflare:git:deploy:public` | `npm run cloudflare:git:deploy:admin` |
| 비운영 브랜치 자동 배포 | 끔 | 끔 |

운영 브랜치는 실제 원격 기본 브랜치 확인 후 동일하게 지정한다. `.node-version`의 Node `24.18.0`과 lockfile을 사용한다. `SKIP_DEPENDENCY_INSTALL=1`을 유지하고 위 빌드 명령의 `npm ci`로 의존성을 설치한다. npm은 `10.9.2` 이상 12 미만, Wrangler는 `4.125.0`이다.

배포 명령 안에서 `cloudflare:build:source`를 먼저 실행한다. 따라서 HTML/CSS 등 공개 산출물은 해당 Git 소스에서 매번 새로 만들어지며 기존 로컬 `dist`나 과거 배포 번들을 재사용하지 않는다. 원본·비공개 파일·실제 사진은 빌드 입력에 필요 없다.

## 배포가 하는 일

`scripts/deploy-cloudflare-git.mjs`는 선택된 Worker 하나에 대해서만 다음을 수행한다.

1. 현재 소스를 새로 빌드한다.
2. 해당 production 설정으로 `wrangler versions upload`를 실행한다. 두 `.jsonc` 설정은 현재 주석 없는 순수 JSON이며 실행 진입점은 이를 JSON으로 읽어 production Worker 이름을 확인한다.
3. Wrangler의 구조화된 `version-upload` 결과에서 Worker 이름·환경·버전 ID를 확인한다. 검증된 업로드 event는 Cloudflare가 지정한 원래 출력 파일(우선) 또는 출력 디렉터리에 JSON Lines 형식으로 추가 전달해 Builds 결과 연결에 사용할 수 있게 한다. 기존 파일을 덮어쓰지 않으며 세션 인자 기록은 전달하지 않는다.
4. 그 정확한 ID를 `wrangler versions deploy <ID>@100% --yes`로 활성화한다.

`wrangler deploy`, `triggers deploy`, D1 migration/import, R2 sync·덮어쓰기·삭제는 호출하지 않는다. 기존 dashboard 변수는 `--keep-vars`로 유지하고, 자원 자동 생성은 끈다. 원래 production 설정의 DB·R2 연결을 사용한다.

Cloudflare가 주입하는 `WRANGLER_CI_OVERRIDE_NAME`과 `WRANGLER_CI_MATCH_TAG`는 제거하거나 바꾸지 않는다. 선택한 Worker와 연결된 이름이 다르면 업로드 전에 중단한다. 관리자 코드를 공개 Worker에 잘못 올리는 것을 막기 위한 대상 확인이다.

## 인증과 아직 확인할 점

공식 문서에서 `CI=true` 자동 주입은 확인했다. 실행 진입점은 추가로 `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`를 요구한다. 이 두 이름의 자동 주입은 공개 설정 문서에서 명시 확인하지 못했으므로, 실제 연결 시 공급 여부를 확인하고 필요하면 Builds 변수/비밀값으로 지정해야 한다. 자동 발급 토큰의 실제 공급 방식·권한과 두 기존 Worker의 연결 상태는 대시보드에서 확인해야 한다. 토큰은 Builds에서 관리하며 Git·문서·로그에 넣지 않는다.

Worker 버전 업로드·활성화 권한이 필요하다. DB·R2 내용 쓰기나 DNS·route 변경 권한은 이 경로의 작업에 필요 없다. 기존 저장소 바인딩 확인에 추가 읽기 권한이 필요한지는 실제 첫 빌드 결과로 확인한다.

두 Builds는 독립 실행된다. 하나가 성공하고 하나가 실패할 수 있으며, 두 Worker를 동시에 바꾸는 원자적 배포를 보장하지 않는다. **둘 다 같은 소스의 배포에 성공하고 운영 상태를 확인해야 전체 전환 완료다.** 활성화 단계 통신 실패는 이미 반영됐을 수도 있으므로 실패 결과만으로 미반영이라 판단하지 않는다. 기존 서비스는 호환 가능한 소스 전환을 전제로 하며 향후 호환성이 깨지는 변경은 별도 순서를 정해야 한다.

## 로컬 확인 범위

`npm run cloudflare:git:test`는 실제 외부 연결 없이 빌드·업로드·활성화 순서, 업로드 실패/잘못된 결과의 활성화 차단, 두 대상 이름 보호, 활성화 실패의 불확실성 보고를 시험한다. 추가로 현재 Git 관리 대상 파일만 임시 공간으로 복사한 소스 빌드를 통과했다. 비공개·원본·로컬 사진을 포함하지 않고 기존 의존성 설치를 공유한 macOS 시험이다. 실제 Linux의 새 의존성 설치·Cloudflare 인증·운영 반영의 증명은 아니다.

## 공식 참고

- [Workers Builds 연결 설정](https://developers.cloudflare.com/workers/ci-cd/builds/configuration/)
- [같은 저장소의 여러 Worker](https://developers.cloudflare.com/workers/ci-cd/builds/advanced-setups/)
- [빌드 대상 이름 일치](https://developers.cloudflare.com/workers/ci-cd/builds/troubleshoot/)
- [빌드 환경과 버전 지정](https://developers.cloudflare.com/workers/ci-cd/builds/build-image/)
- [버전 업로드·활성화 명령](https://developers.cloudflare.com/workers/wrangler/commands/workers/)
