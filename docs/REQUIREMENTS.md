# dwnc.me 현재 요구사항

최종 갱신: 2026-09-12 KST

현재 유효한 미완료 요구사항만 관리한다. 완료 항목·완료 계획은 [`REQUIREMENTS_ARCHIVE.md`](REQUIREMENTS_ARCHIVE.md)로 옮기며 평소에는 읽지 않는다. 현재 운영 상태는 [`PROJECT_STATE.md`](../PROJECT_STATE.md), 미디어 기술 규칙은 [`MEDIA_SERVING_CONTRACT.md`](MEDIA_SERVING_CONTRACT.md)가 정본이다.

<!-- requirements-policy: current-status=unfinished -->

## 한눈에 보는 진행 상황

### 최종 결과

기존 글·서식·사진·공개 주소를 보존하면서 블로그를 지속 운영한다. 별도로 등록한 결함 6건은 착수 판단을 기다린다. 세부 결함 범위와 완료 조건은 [결함 보완 요구사항](결함_보완_요구사항_20260909_223512.md)에 둔다. 이미 완료된 기능과 배포 결과는 완료 원장과 공식 상태에서 확인한다.

### 전체 계획

현재 유효한 계획만 표시한다. 완료된 `PLAN-01`~`PLAN-08`의 정의와 참조는 [완료 계획](REQUIREMENTS_ARCHIVE.md#완료-계획)에 보존한다.

| 계획 ID | 목적과 주요 작업 | 완료 기준 | 현재 상태 |
|---|---|---|---|
| `PLAN-00` | 실행 단계가 아닌 경량 기록 원칙으로 목표와 현재 위치를 관리한다. | 최종 결과·완료조건·범위·순서·승인 범위가 실제로 달라질 때만 요구사항과 상태를 갱신하고, 단순 질문·설명·기존 작업 진행 지시에는 새 번호를 만들지 않는다. | `계속 적용` |
| `PLAN-09` | 로그인형 편집기를 지속 운영할 CMS로 완성하고 공개 화면과 새 글·사진 보관을 보완한다. | 작업본 자동저장과 명시적 공개 반영을 분리하고 저장 실패·로그인 만료·수정 충돌에도 작성 내용을 유지한다. 기존 편집·이미지·미리보기·주소·597부터의 발행을 유지하고 기본 서식 도구와 기존 글 서식 보존, 티스토리를 참고한 집중 작성 화면을 제공하며 공개 화면과 탐색을 보완하고 독립 사본 한 곳을 마련한다. | `진행 중` |

### 현재 위치

**현재 승인된 구현 작업 없음.** `DWNC-CORE-012`는 별도 착수 지시 전까지 `planned`를 유지한다. 진행 중인 계획은 `PLAN-09` 하나다.

사용자에게 이는 요청한 개선은 운영에 반영됐고, 남은 결함·후속 선택은 별도 지시를 기다린다는 뜻이다.

### 아직 결정할 일과 진행을 막는 조건

- 기존 결함 6건 보완은 사용자의 후속 착수 지시를 기다린다.
- 새 글·사진의 독립 사본 위치와 비공개 백업 정책, 공유·스크랩 10개 처리는 별도 선택으로 남는다.
- 접근성·추가 탐색 등 후속 개선은 사용자가 요청할 때만 진행한다. 댓글·방명록·구독과 팀블로그는 현재 제외, 광고는 후순위다.

### 바로 다음 작업

다음에는 사용자가 남은 결함이나 후속 선택을 지시할 때 해당 범위만 진행한다. 사용자가 별도 결함 보완을 지시하면 `DWNC-CORE-012`의 합성 재현부터 시작한다. 새 비공개 합성 시험 글은 현재 상태로 보존하며 공개·삭제하지 않는다.

### 이번 작업에서 하지 않는 것

- 별도 착수 지시 전 `CORE-012` 결함 6건의 구현·신규 시험·빌드·운영 반영
- 승인 없이 실제 `dwnc.me`의 주소 연결 설정과 방문자 흐름을 다시 바꾸는 일
- 기존 글·작업본·원본 사진을 시험 목적으로 변경·삭제하거나 사진 저장소를 덮어쓰는 일
- 이번 Git 연결 범위 밖의 외부 전송과 승인된 배포 경로를 우회하는 직접 배포
- 비밀값·비공개 콘텐츠·개인 식별값을 문서나 작업 기록에 남기는 일

### 새 요청 반영 방법

1. 목표·완료조건·범위·순서·승인 범위가 실제로 달라질 때만 요구사항 번호·계획 ID·우선순위·상태·근거를 갱신한다. 단순 질문과 기존 작업의 계속 지시에는 새 번호를 만들지 않는다.
2. 실제 계획이 바뀌면 `현재 위치`·`바로 다음 작업`과 공식 상태를 함께 갱신한다. 설명이나 향후 약속만으로 반영을 완료했다고 하지 않는다.
3. 완료 항목은 현재 문서에 남기지 않고 기존 보관 문서로 이동한다. 완료된 상세 요구사항도 보관 영역으로 이동하고 링크를 갱신한다. 안정 ID는 보존하며 재사용하지 않는다.

### 기술 참고

실행 근거는 [`PROJECT_STATE.md`](../PROJECT_STATE.md), 결함 근거는 [운영자 상황별 점검](운영자_상황별_대응_점검_20260909_223042.md)을 필요한 부분만 조회한다. 과거 완료 이력은 현재판에 반복하지 않는다.

## 요구사항 원장

### `DWNC-CORE-012` — 운영자 점검에서 발견한 기존 기능 결함 보완
- **Status:** `planned`
- **Updated-at:** `2026-09-09`
- **Plans:** `PLAN-09`
- **Priority:** `P1`
- **Acceptance:**
  - 현재는 요구사항 등록만 완료하고 사용자의 후속 착수 지시 전에는 구현·신규 시험·빌드·배포하지 않는다. 세부 요구와 인수 조건은 [결함 보완 요구사항](결함_보완_요구사항_20260909_223512.md)을 따른다.
  - F01(A33): 삭제한 사진을 실행 취소·다시 실행으로 원래 위치·서식과 함께 복원·제거하고 주변 편집 이력과 작업본/공개 반영 분리를 유지한다.
  - F02(L18/O23): 반영된 CSS 배경 사진을 공개·예약·보호 정책과 글 소유권에 맞게 제공한다. 비공개·미반영·다른 글 소유 사진은 차단한다.
  - F03(O07): 검색 통신 실패를 결과 없음과 구별해 안내하고 새로고침 없이 같은 검색어를 재시도해 회복한다.
  - F04(O39): 검색어 삭제·변경 후 늦은 이전 결과나 오류가 최신 입력의 화면을 덮지 않는다.
  - F05(A16): 초안 생성 성공 뒤 응답 유실과 동시 재시도를 합성으로 확인하고 같은 요청은 같은 초안 하나로 이어지게 한다. 새로운 작성 의도와 요청 내용 불일치를 구별한다.
  - F06(A28): 아직 미실행인 업로드 응답 유실 합성 재현부터 확인하고, 사진/DB 저장 단계 실패·동시 재시도에서도 같은 요청의 객체·미디어 행·본문 삽입을 중복하지 않는다. 소유권·인증·파일 일치 검사를 유지한다.
  - 향후 착수 후 여섯 결함의 재현 해소와 관련 회귀·합성 브라우저 확인을 마치고 최종 소스로 새 빌드한 전체 보완을 운영 반영·읽기 전용 확인한다. 일부만 먼저 배포하거나 이전 번들을 사용하지 않는다.
  - 원본·기존 글·작업본·서식·사진·공개 주소를 보존한다. Git push/raw deploy/DNS·route 변경/R2 덮어쓰기·삭제/실제 글 시험 변경·삭제/비밀·비공개 콘텐츠 기록 금지를 유지한다. 백업·휴지통 등 별도 기능과 HTML 삽입 실행 취소 미확인은 이번 범위에 추가하지 않는다.
- **Evidence:**
  - [운영자 상황별 점검](운영자_상황별_대응_점검_20260909_223042.md)의 결함 행과 현재 소스를 대조했다. CSS L18/O23은 같은 결함이고 A16/A28은 별도 저장 경로이다.
  - F01~F04는 합성 재현 근거, F05는 같은 합성 생성 호출의 중복 확인과 전송 유실 경로 추론, F06은 소스 확인만 있다. 현재 여섯 항목 모두 현실적 수정 방향이 있고 불가능·과도한 난도로 제외할 근거는 없다. 이번 기록은 수정 완료나 실행 승인이 아니다.

### `DWNC-OPS-001` — 공유·스크랩 추정 10개 처리 결정
- **Status:** `decision-needed`
- **Updated-at:** `2026-08-26`
- **Plans:** `PLAN-09`
- **Priority:** `P2`
- **Acceptance:**
  - 10개 각각을 링크형 기록 또는 제외로 결정한다.
  - 원문 복제나 공개 registry·검색·RSS·sitemap 편입은 권리와 공개 범위를 증명한 경우에만 허용한다.
  - 사용자가 이 후속 선택을 요청하기 전에는 진행하지 않으며 Stage 3 완료를 막지 않는다.
- **Evidence:**
  - [`PROJECT_STATE.md`](../PROJECT_STATE.md)에 공개 목록과 직접 작성 목록 차이 10개가 미해결로 기록돼 있다.

### `DWNC-OPS-003` — 비공개 백업 정책과 댓글 제외 결정
- **Status:** `decision-needed`
- **Updated-at:** `2026-08-26`
- **Plans:** `PLAN-09`
- **Priority:** `P1`
- **Acceptance:**
  - 최종 운영에 필요한 private raw·본문·미디어의 암호화 백업과 복구 검증 절차를 정한다.
  - 댓글 기능은 사용자 결정에 따라 제공하지 않는다. 기존 보존 원본은 삭제하지 않는다.
  - private 콘텐츠나 identity를 공개 저장소·로그에 노출하지 않는다.
- **Evidence:**
  - 2026-09-08 사용자 결정으로 댓글은 제외했다. 암호화 백업 정책은 [`PROJECT_STATE.md`](../PROJECT_STATE.md)의 후속 미해결 항목으로 남는다.

### `DWNC-OPS-004` — 비개발자도 이해하기 쉬운 진행 설명
- **Status:** `in-progress`
- **Updated-at:** `2026-08-27`
- **Plans:** `PLAN-00`
- **Priority:** `P0`
- **Acceptance:**
  - 모든 진행 상황, 질문과 결과 보고를 비개발자도 한 번에 이해할 수 있는 일상적인 한국어로 먼저 설명한다.
  - 먼저 무엇을 확인하거나 완료했는지, 사용자에게 어떤 의미인지, 다음에 무엇을 하는지를 차례로 말한다.
  - 명령어, 내부 함수·파일 형식, 긴 확인 번호와 요청 횟수 같은 기술 정보는 사용자가 요청했거나 검증에 꼭 필요한 경우에만 뒤쪽 `기술 참고`로 분리하고, 필요한 경우에도 바로 쉬운 뜻을 함께 설명한다.
  - `gate`, `NO-GO`, `별도 금지선`, `fail-closed`, `exact tree`처럼 번역투이거나 조직 내부에서만 통하는 표현을 사용자에게 그대로 쓰지 않는다.
  - 사용자가 직접 해야 할 일이 있으면 버튼 이름과 누르는 순서를 짧고 구체적으로 안내한다.
- **Evidence:**
  - 사용자가 2026-08-27에 이 기준을 요구사항 문서와 앞으로의 모든 대화에 계속 적용하도록 명시했다.
  - 이 문서의 첫 화면은 결과, 사용자에게 미치는 의미, 다음 작업을 쉬운 한국어로 먼저 보여 주고 긴 확인 번호는 `기술 참고`로 분리한다.
  - `npm run requirements:validate`는 이 상시 요구사항과 사용자용 설명 순서가 빠지거나, 쉬운 설명 없이 금지한 기술 표현만 쓰인 경우를 거부한다.

### `DWNC-OPS-005` — 중요한 변경만 전체 계획과 연결
- **Status:** `in-progress`
- **Updated-at:** `2026-09-12`
- **Plans:** `PLAN-00`
- **Priority:** `P0`
- **Acceptance:**
  - 최종 결과·명시적 완료조건·범위·순서·승인 범위가 실제로 바뀔 때만 요구사항 번호와 계획 ID를 연결하고 우선순위, 완료 기준, 상태와 근거를 기록한다.
  - 단순 질문·설명·진행 확인·이미 기록된 작업의 계속 지시는 새 번호를 만들지 않는다.
  - 실제 계획이 바뀔 때만 `현재 위치`와 `바로 다음 작업`을 같은 변경에서 고친다.
  - 대화 내용만 공식 상태로 삼지 않고 `REQUIREMENTS.md`와 `PROJECT_STATE.md`를 함께 갱신한다.
  - 현재 유효한 미완료 항목만 유지하고 완료 항목은 모두 보관 문서로 옮긴다. 완료 상세 요구사항도 보관 영역으로 이동하며 안정 ID와 참조를 보존한다.
  - 작업 마감·인수인계 전 저장소 관리 대상인 코드·지침·요구사항·상태·이력·검사 파일의 변경을 모두 커밋하고, 원격 연결이 가능하면 GitHub에 푸시한다. 비밀값·비공개 원본 등 기존 제외 대상은 유지한다. 다른 컴퓨터에서 최신 소스와 공식 기록으로 이어갈 수 있어야 하며 문서를 로컬에만 남기지 않는다.
- **Evidence:**
  - 사용자 요청(2026-09-09)에 따라 완료 항목을 현재 원장에 누적하지 않는다. `PLAN-00`·`PLAN-09`는 현재판에, 완료 계획은 보관 문서에 두고 기존 계획 참조를 유지한다.
  - 지속해서 관리할 현재 요구사항의 `Plans` 항목을 계획표와 연결했다. 사소한 대화는 새 요구사항으로 늘리지 않는다.
  - `npm run requirements:validate`는 이 상시 요구사항, 계획표, 현재 위치 한 개와 요구사항별 계획 연결이 빠지거나 잘못되면 거부한다.

  - 2026-09-12 사용자가 다른 컴퓨터에서 이어가기 위해 모든 저장소 관리 대상 변경을 커밋하고 가능하면 푸시하도록 정정했다. AGENTS.md와 공식 상태에 같은 기준을 반영했다.

### `DWNC-OPS-006` — 브라우저 작업은 Codex 자체 브라우저만 사용
- **Status:** `in-progress`
- **Updated-at:** `2026-08-28`
- **Plans:** `PLAN-00`, `PLAN-05`, `PLAN-06`
- **Priority:** `P0`
- **Acceptance:**
  - 이 프로젝트의 앞으로 모든 브라우저 작업은 Codex 자체 브라우저에서만 하고 Chrome과 Edge를 사용하지 않는다.
  - 자체 브라우저의 로그인 상태가 프로젝트·작업마다 분리된다고 가정하지 않는다. 같은 사이트에서 서로 다른 계정을 쓰는 프로젝트의 브라우저 작업은 동시에 하지 않고 하나씩 진행한다.
  - 브라우저에 로그인돼 있다는 사실은 올바른 계정의 증거로 사용하지 않는다. Cloudflare 작업을 시작할 때 화면의 `Account ID` 또는 그 확인값이 이 프로젝트에 안전하게 저장된 계정 확인값과 일치하고, 일치하는 대상 계정이 정확히 한 개뿐인지 한 번 확인한다. 같은 세션과 탭에서는 반복하지 않고, 브라우저 세션·탭·프로젝트가 바뀌거나 계정 상태가 달라졌을 때만 다시 확인한다. 다르거나 확인할 수 없으면 설정 조회·변경과 열쇠 생성 전에 멈추고 사용자가 직접 로그아웃·로그인한다.
  - 로그인이 필요하면 Cloudflare 공식 로그인 페이지를 열고 사용자가 로그인 정보와 비밀번호를 직접 입력한다.
  - 로그인 정보와 비밀번호를 문서·로그·화면 출력·프로젝트 파일에 저장하지 않는다.
  - 다음 계정 확인은 자체 브라우저에서 올바른 Cloudflare 계정인지 먼저 확인하고 `Account ID` 옆 `Copy`를 한 번만 누른다.
- **Evidence:**
  - 사용자가 2026-08-28에 앞으로의 브라우저 사용 경로와 로그인 정보 보존 금지를 명시했다.
  - 같은 날 사용자가 서로 다른 프로젝트에서 같은 사이트의 다른 계정을 쓸 때 이전 로그인 상태가 이어질 가능성을 확인해 달라고 요청했다. 프로젝트별 로그인 분리를 보장하는 근거가 없고, 이 프로젝트 안에서도 서로 다른 자체 브라우저 작업에서 Cloudflare 로그인 상태가 같지 않았으므로 로그인 상태 자체는 계정 확인 근거로 쓰지 않는다.
  - 자체 브라우저 화면의 계정을 이 프로젝트 확인값과 한 번 대조해 정확히 일치함을 확인했다. 같은 세션과 탭에서는 반복 확인하지 않는다.
  - 이전 Chrome 관측 기록은 과거 증거로만 보존하고 이후 작업 경로로 재사용하지 않는다.

### `DWNC-P2-001` — 후속 접근성·탐색 개선
- **Status:** `planned`
- **Updated-at:** `2026-08-26`
- **Plans:** `PLAN-09`
- **Priority:** `P2`
- **Acceptance:**
  - 공개 이미지 대체텍스트를 원본 불변 범위의 파생 정책으로 개선한다.
  - tag 660개의 추가 탐색과 검증된 fragment map을 별도 설계한다.
  - 종료된 티스토리 영상 fallback의 표시 문제를 파생 표현에서 보완한다.
  - 이 항목들은 현재 프로젝트 완료조건과 Stage 3에서 제외하며 사용자가 요청할 때만 진행한다.
- **Evidence:**
  - [`PROJECT_STATE.md`](../PROJECT_STATE.md)의 P2 접근성·tag·fragment·fallback 항목.
