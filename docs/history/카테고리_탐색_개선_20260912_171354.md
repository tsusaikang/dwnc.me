# DWNC-CORE-016 카테고리 탐색 개선

## 목표·결정

- 사용자 2026-09-12 추천안 수락 및 최종 소스 Git 자동 배포·운영 확인 승인.
- 용어 ‘카테고리’, 부모 행 전체 펼침/접기·꺾쇠, 첫 ‘카테고리명 전체 보기’, 두 일상 분류 표시 통합.
- `category-display.ts`의 표시 규칙으로 `daily-stories`를 `daily`와 함께 탐색한다. 원본 TAXONOMY/CmsCategory·categoryId·기존 글/작업본/공개 상태/주소는 변경하지 않는다.
- 관리자 글 목록 필터와 편집 선택에는 ‘일상’ 한 항목. 기존 `daily-stories` 글을 저장하면 원래 ID를 유지한다. 원본 구조를 편집하는 카테고리 관리 화면의 원본 항목은 보존한다.

## 구현·산출물

- `src/lib/category-display.ts`: 표시 ID·라벨·노드·원본 ID를 유지하는 편집 선택.
- 정적 `CategoryBranch`, `CategoryDrawer`, `CategoryListing`, 헤더/푸터, 글 레이아웃, taxonomy·검색·홈·카테고리와 `native-public-worker.ts` 양쪽 반영.
- `admin-ui.ts`, `admin-management.ts`, `admin-worker.ts`: 선택/목록 표시 및 통합 필터. 저장 API의 원본 분류 값은 그대로 사용.
- 회귀 `scripts/test-category-display.mjs`, 기존 native editor·taxonomy build 검사 갱신, 선택적 로컬 `DWNC_CATEGORY_FIXTURE=1`.
- 기존 생성 규칙으로 공개 요청 경로 목록을 갱신했다. 추가된 통합 카테고리 페이지와 새 JS/CSS에 따른 변경이다.
- 기능과 관련 시험 23파일을 `409161f5fd4f6017eb120a56ef9862037afaed82`로 커밋·원격 main에 push. 기존 문서·보관 이동·`test-requirements-validator.mjs` 미커밋 변경은 보존.

## 검증

- 하위 구현 에이전트: 최종 editor:test 전체 및 category-display 합성 회귀, source-only 빌드 통과. 오류·경고0, 기존 힌트21, dist 미디어 사본0·비공개 누출0.
- 독립 하위 검토: 원본 분류·기존 글 categoryId·작업본·공개본·기존 주소·저장된 메뉴명 표시·정적 검색 fallback 검토, 차단 결함 없음.
- 메인: Git 자동 배포 진입점 합성 시험·diff 검사 통과.
- 내장 브라우저 로컬: 합성 기존 daily-stories 작업본 제목 수정→자동저장→페이지 새로고침→재열기에서 ‘일상’ 선택 하나 및 실제 선택값 daily-stories 유지.
- 정적 카테고리 화면: 일상108편·root7, 수영 부모 행 클릭·Space 접기·Enter 펼치기·첫 수영 전체 보기117편 이동·현재 분류 자동 펼침.
- 390×844 모바일 카테고리 패널: 부모 전체 행·첫 전체 보기·수영 일기 현재 위치 표시·레이아웃 확인. 실제 휴대폰 터치 실기는 아니다.
- 원본 글 본문과 원본 분류를 일괄 치환하지 않았다. 로컬은 기존 공개 정적349편, 운영은 별도 신규 글을 포함해350편으로 관측되며 차이를 없애기 위한 데이터 변경은 하지 않는다.

## 운영·조회 통계

- 내장 Cloudflare 대시보드의 계정 ID를 프로젝트 확인값과 대조해 일치 확인. 같은 탭에서 공개/관리자 배포를 조회하며 권한·연결·DNS·route·D1/R2 데이터 변경 없음.
- 운영 확인 시작 전(17:00:50 KST) 오늘·최근30일·누적 조회수 각각4.
- 홈·카테고리·관리자는 집계 대상 아님. 공개 본문이 필요하면 기존 `?preview=1` 제외 경로만 사용. 조회 정책 추가 변경·통계 재초기화는 하지 않는다.
- 공개·관리자 양쪽 Builds에서 동일 소스 `409161f` Success 확인. 배포 이력에서 공개 `c6adc850`, 관리자 `e373edbb` 각각100% 활성화 확인.
- 운영 공개 카테고리 화면 root7·일상108편, 상하단 카테고리 용어 확인. 부모 수영 행 전체 클릭으로 펼침, 첫 수영 전체 보기→117편 목록 이동 확인.
- 두 이전 URL `/category/일상`·`/category/일상-이야기`는 각각 제목 일상·108편이고 첫 페이지15개 글 주소가 일치한다. 리다이렉트·분류 덮어쓰기 없이 기존 URL을 유지한다.
- 운영 관리자 글 찾기에서 일상 선택 한 항목(value daily)과 선택 동작을 확인했으며 기존 글을 열거나 저장하지 않았다.
- 운영 수영 일기 페이지에서 수영 부모 자동 펼침·수영 일기 현재 위치 표시를 확인했다. 운영 모바일 크기 확인은 도구가 선택된 Cloudflare 탭에 크기를 적용해 대상 페이지 실제260px으로 관측됐으므로 390px 운영 폭 검증으로 주장하지 않는다. 260px에서는 기존320px 최소폭 때문에 가로 넘침이 관측됐다. 로컬390px 패널은 위 스크린 확인을 따른다. 임시 크기 설정은 복원했다.
- 17:18:21 KST 최종 운영 조회수는 오늘·30일·누적 각각15로 시작4에서 증가했다. 이번 운영 브라우저 탐색은 카테고리/관리자/Cloudflare에만 한정하고 공개 글 본문은 열지 않았다. 코드 및 합성 시험상 이 경로는 집계하지 않는다. 현재 통계는 요청 주체를 기록하지 않으므로 증가11회의 출처는 확인할 수 없고, 이를 개발 확인 접속이라고 단정하거나 재초기화하지 않았다.
- 메인은 사용자 요청대로 브라우저·통합·Git push·공식 상태, 하위 에이전트는 구현·로컬 시험·독립 읽기 검토를 담당했다. 로컬 합성/공개 미리보기 서버는 종료했다.
- 요청 범위 완료. 기존 결함 CORE-012와 별도 백업·독립 사본 등 후속 선택은 착수하지 않는다.
