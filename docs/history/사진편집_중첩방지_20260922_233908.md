# 사진 편집 중첩 방지와 한정 작업본 정리

- 요구사항: `DWNC-CORE-029`, `PLAN-09`.
- 상태: 2026-09-23 마감 기준 재발 방지·양쪽 배포·최신 전체 글 조사·#599 한정 작업본 정리 완료. 정리 작업본은 미공개이며 #598 비공개 합성 시험 글은 조사만 했다.

## 요청과 이번 운영 조회

사용자는 #599에서 사진 편집 적용 시 테두리가 반복해 늘고 텍스트가 그 안으로 들어갔다고 알렸다. 텍스트는 직접 바깥으로 꺼내 저장했다고 설명했다.

메인이 이번 작업을 위해 최신 운영 API와 관리자 DOM을 읽었다. 마지막 저장 23:36:05 KST이며 당시 작업본과 공개 내용이 같았다. 빈 figure 2개와 문단 3개, 같은 형태의 figure 4겹 중첩을 확인했고 가장 안쪽에는 사진 1개와 캡션 1개가 있었다. 이 구조는 조회 시점 관측이며 실제 정리 직전에 다시 조회한다. 운영 고유 제목·본문·이미지 경로는 문서·Git에 기록하지 않는다.

## 수정 범위와 보존

사진 편집 적용은 선택한 정확한 DOM 요소를 교체하도록 고친다. `execCommand('insertHTML')`로 선택 영역에 다시 삽입하면서 기존 figure 안에 새 figure가 들어가는 경로를 보완한다. 캡션을 바꾸지 않은 경우 기존 캡션 내부 서식을 유지하고 사진 뒤 텍스트·주변 문단도 보존한다.

재발 방지 확인과 운영 반영 뒤 해당 글의 최신 작업본·반영본을 다시 조회하고 현재 revision을 기준으로 확정된 빈 틀 2개·중복 바깥 틀 3개만 정리한다. 사용자 수정으로 구조가 달라졌으면 기존 숫자를 강제하지 않는다. 가장 안쪽 사진·캡션과 사진 밖 본문·기존 서식을 보존한 작업본만 저장하고 공개 반영하지 않는다. 다른 글·원본 사진·공개 상태는 변경하지 않는다.

## 역할과 확인 범위

구현과 독립 읽기 검토는 하위 에이전트가 맡고 메인은 최신 운영 조회·사용자 세션에 묶인 브라우저·통합·Git 배포·해당 작업본 정리를 맡는다.

로컬 합성 재현·반복 적용·실행 취소/다시 실행·저장/재열기·사진 뒤 텍스트를 확인하고 최종 소스를 기존 Git 자동 배포로 양쪽에 반영한 뒤 운영을 읽기 확인한다. 실제 글로 재현 시험을 하지 않는다. 코드 수정 완료와 해당 글 작업본 정리 완료를 구분해 기록하며 현재 위치·완료조건은 [공식 상태](../../PROJECT_STATE.md) 및 [완료 원장](../REQUIREMENTS_ARCHIVE.md)을 따른다.

## 추가 요청: 다른 글의 같은 문제 전수조사

사용자가 수정 후 다른 글도 전수조사하도록 요청했다. 이번 작업 중 운영 DB/API에서 조회한 현재 전체 목록과 모든 작업본·공개본을 기준으로 새 글·사용자 변경분을 포함하고 조회 시점·범위를 기록한다. 정상적인 중첩·사진 묶음·캡션과 손상 징후를 구분해 확정 문제·검토 필요·접근 미확인으로 보고한다.

다른 글 일괄 수정은 조사 승인에 포함되지 않는다. 운영 자료를 읽지 못한 범위는 실제 장애와 함께 미확인으로 남기고 과거 이관 덤프로 대신하지 않는다. 해당 글의 확정된 빈 틀·중복 바깥 틀 작업본 정리는 기존 범위로 진행한다. 운영 고유 내용 없이 조사 결과와 남은 판단만 공식 기록에 남긴다.

## 로컬 재현·수정 후 확인

메인이 내장 브라우저의 로컬 합성 편집기에서 사진과 카드가 인접한 구조를 확인했다. 수정 전 사진 편집을 한 번 적용하면 figure 수가 2개에서 3개로 늘고 중첩 figure가 2개 생기며 사진이 카드 안으로 들어가는 현상을 재현했다.

수정 후 같은 조건에서 캡션 변경을 네 번 적용해도 figure 2개·중첩 0을 유지했다. 사진 뒤 `x` 입력이 사진 밖에 남고 실행 취소/다시 실행·자동저장·새로고침 후에도 구조·캡션·뒤 입력을 유지하는 것을 확인했다. 이는 로컬 합성 자료 시험이며 운영 글을 재현 시험에 쓰지 않았다.

코드 담당의 관련 검사는 통과했다. 이 로컬 확인 시점에는 최종 빌드 대기·D1 첫 인증 오류 `7403` 해결 중이었으며 이때 전체 목록·작업본·공개본 검사를 완료했다고 판정하지 않았다. 이번 로컬 재발 방지 확인을 운영 배포·해당 글 작업본 정리·전수조사 완료로 확대하지 않는다.

## 최종 소스 빌드·운영 배포 확인

최종 소스 빌드의 첫 실행은 비공개 자료 루트 생성 충돌로 실패했다. 재실행은 통과했고 Astro 오류·경고 0, 비공개 자료 누출 검사 0을 확인했다. 최초 실패와 재실행 성공을 구분하며 첫 실행부터 성공한 것으로 기록하지 않는다.

기능 소스 `c8f867d`를 커밋·원격 푸시했고 같은 소스의 관리자 빌드 `487a17a1-c345-4084-8598-4c3f18221fb9`와 공개 빌드 `ad9073a7-9bbc-4435-9b67-a7fef8296d80`가 모두 Success임을 메인이 확인했다. 운영 관리자 새로고침 후 실행 소스에 정확한 대상 교체 `extraTarget.replaceWith(fragment)`가 반영된 것을 읽기 확인했다.

#599를 새로 열어 확인한 기존 작업본은 figure 6개·중첩 3개이고 마지막 저장 23:36:05 KST, 작업본과 반영본이 같은 상태였다. 코드 배포만으로 기존 저장 구조가 자동 정리되지는 않았다. 이는 한정 정리 전 관측이다. 이후 아래 전체 재조회와 작업본 정리를 완료했으며 배포만으로 복구됐다고 간주하지 않았다.

## 최신 전체 조사 결과

명령행 D1 조회의 최초 인증 오류 `7403` 이후, 메인이 로그인된 Cloudflare D1 Console에서 현재 자료를 SELECT하고 렌더링된 DOM 전체 601행을 읽었다. 현재 601글(공개 구분 353·비공개 248)의 반영본 601개와 별도 작업본 335개, 총 936본문·13,794,671문자를 검사했다. 이번 운영 조회가 입력이며 과거 이관 덤프를 대신 사용하지 않았다.

순수 JavaScript 태그 스택 구조 분류 후 후보의 실제 구조를 수동으로 검토했다. figure 836개·layout 4개를 대상으로 정상 사진 그룹·캡션과 중복 틀을 구분했다. 추가로 같은 936본문의 네이버 사진 DIV(`.se_component.se_image`·`.se-component.se-image`) 5,728개를 검사해 내부 동일 사진 DIV 중첩 0·같은 class의 단일 자식과 사진 1개로 이루어진 중복 후보 0을 확인했고, 이 figure 외 사진 대상도 이번 `replaceWith` 교체 경로를 사용함을 확인해 조사 범위를 보완했다.

| 분류 | 결과 |
|---|---|
| 확정 — #599 | 반영본과 작업본 각각 빈 figure 2개·중복 바깥 figure 3개. 아래 작업본 한정 정리를 적용했다. |
| 확정 — #598 | 기존 비공개 합성 사진 시험 글이며 반영본과 작업본 각각 중복 figure 1개. 그룹 항목 안 동일 figure 중첩을 나머지 정상 그룹 사진 2개와 구분했다. 조사만 했고 수정하지 않았다. |
| 다른 실제 글 | 같은 손상 징후 0. 다른 중첩·텍스트가 틀 안에 들어간 후보도 0. |
| 검토 필요·접근 미확인 | 이번 전체 조회 범위에서 남은 후보·접근 미확인 없음. |

이후 전체 601글의 revision·상태·공개 구분·작업본 revision 등 메타데이터를 재조회해 조사 중 변경이 없음을 확인한 뒤 #599만 정리했다. 원문 파일 다운로드는 도구에서 지원되지 않아 CUA 런타임에서 읽고 분석했으며 원문 파일 사본을 확보했다고 주장하지 않는다. 비공개 원문·제목·이미지 경로는 문서나 Git에 보관하지 않았다. 제목·본문 없는 수량·후보 번호·방법 요약은 Git 제외 `migration/private/core029_20260922_234243/browser-audit-summary.json`과 `browser-audit-reports.json`에 둔다. 조사 자체의 DB 쓰기는 0이며 이후 승인된 #599 작업본 정리와 구분한다.

## #599 작업본 정리와 완료 판정

2026-09-22 23:59:46.817 KST(`2026-09-22T14:59:46.817Z`)에 기존 revision 충돌 방식을 사용해 확정된 빈 figure 2개와 순수 중복 바깥 figure 3개만 제거했다. 기존 문단 3개와 가장 안쪽 사진 figure의 정확한 DOM을 유지해 저장했다. 공개 반영본 revision 61은 유지하고 작업본 revision 62가 됐다.

관리자 새로고침 후 기대 DOM과 정확히 일치했고 figure 1개·중첩 0·문단 3개·사진 1개·캡션 1개를 확인했다. D1 신규 조회에서 공개 본문은 정리 전 원문과 정확히 같았다. 작업본은 `br`·`img`의 self-closing void tag `/>` 표기만 정규화한 뒤 기대 본문과 정확히 같았다. 원본 사진·캡션·본문·서식을 보존했고 공개 반영 버튼은 누르지 않았다. #598을 포함한 다른 운영 글은 변경하지 않았다.

재발 방지 코드·관련 회귀·로컬 합성 동작·최종 빌드·동일 소스 양쪽 배포·운영 코드 반영, 최신 전체 조사와 승인된 한정 작업본 정리를 완료해 `DWNC-CORE-029`를 완료 원장으로 옮겼다. 남은 선택은 정리된 #599 작업본을 공개 내용에 반영할지 여부이며, #598 시험 글의 중복은 조사 결과로만 남는다. 실제 운영 글에서 사진 편집 재현 입력을 하지 않았다는 확인 범위는 유지한다.
