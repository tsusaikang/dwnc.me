# 신규 사진 업로드 최적화와 기술 설명 글 초안

- 요구사항: `DWNC-CORE-031`, `PLAN-09`.
- 상태: 2026-09-23 02:16 KST 양쪽 자동 배포·운영 화면 반영과 미공개 글 본문·도식 저장/재열기/PC 미리보기까지 완료. 글은 공개하지 않았다.

## 목표와 실행 범위

사용자가 앞선 투명도·압축 검토에 “그렇게 처리해줘”라고 실행을 요청했고 별도로 브라우저 기술 원리의 블로그 글 작성도 요청했다. 신규 업로드 기능과 실제 미공개 글 초안의 두 결과를 완성한다. 기존 사진 전수 변환·삭제나 Google 포토 설정·요금제 변경으로 범위를 넓히지 않는다.

첨부와 클립보드의 공통 업로드 경로에서 정지 불투명 이미지를 JPEG로 재압축하고 최대 16MP 이내로 비율을 유지한다. 실제 투명 PNG·애니메이션을 보존하고 작게 압축된 입력을 불필요하게 키우지 않는다. 사용자 로컬 파일을 바꾸지 않으며 업로드할 결과 바이트의 MIME·크기·SHA-256을 일치시킨다. JPEG 품질은 로컬 실제 브라우저 시험을 거친 0.85로 적용하고 평균 약 1MB를 보장값으로 설명하지 않는다.

## 역할·자료와 완료 확인

코드 담당은 공통 업로드와 회귀, 글 담당은 원고·필요 도식, 메인은 사용자 세션의 브라우저·통합·Git 배포·운영 읽기 및 실제 초안 저장을 맡는다. 문서 담당은 공식 상태·완료조건·결과를 갱신한다.

관련 회귀와 로컬 실제 canvas로 품질·크기·해상도·투명도·애니메이션·입력 경로를 확인한 뒤 최종 빌드와 양쪽 Git 자동 배포·운영 읽기를 마친다. 신규 기능 시험은 로컬에서 우선하며 기존 실제 글을 시험 수정하지 않는다.

기술 설명 글은 구현 결과와 확인 한계를 반영해 필요한 도식과 함께 미공개 초안으로 저장하고 공개하지 않는다. 작성 자료 위치는 Git 제외 `migration/private/upload_optimization_article_20260923/`다. 이는 사용자 요청 콘텐츠 작성으로 기존 비공개 합성 시험 글과 구분한다. 개인 초안 본문은 공식 문서나 공개 저장소에 옮기지 않는다.

정책 검토는 [기존 참고 문서](../사진저장_조회비용_20260923_005427.md), 완료조건은 [완료 원장](../REQUIREMENTS_ARCHIVE.md), 다음 행동은 [공식 상태](../../PROJECT_STATE.md)를 따른다. 운영 기준선과 최신 목록 수는 아래 최종 조회 결과를 따른다.

## 구현 정책과 당시 확인 단계

`src/lib/admin-image-upload.ts`의 공통 준비 로직을 읽고 미디어 계약에 반영했다. 정지 불투명 입력의 JPEG 품질은 `0.85`, 최대 화소는 16,000,000이며 비율을 유지하고 확대하지 않는다. 축소하지 않은 JPEG 결과가 입력 이상이면 작은 불투명 PNG를 포함해 원본 입력을 사용해 팽창을 막는다. 이 정책을 처음 기록할 당시에는 실제 브라우저·빌드 검증 중이었으며 완료 결과는 아래에 별도로 기록했다.

JPEG 외 이미지의 알파는 원래 해상도에서 최대 1024×1024 타일로 검사한다. 실제 투명 PNG는 16MP 이내면 바이트를 유지하고 초과 시 PNG로 축소하며 다른 정지 투명 이미지는 PNG로 출력한다. 16비트 PNG의 알파/tRNS는 canvas의 8비트 반올림 오판을 피하려고 원형을 보존한다. GIF·APNG·애니메이션 WebP·애니메이션/시퀀스 AVIF도 원형을 유지한다. 정지 AVIF 전체를 무조건 제외하는 정책은 아니다.

입력 25MiB 한도와 오류 시 해당 파일 미업로드를 유지하고 최종 파일의 MIME·이름·바이트 수·SHA-256이 업로드 결과와 맞도록 연결한다. 기존 사진 일괄 변환·삭제는 없으며 글 초안의 실제 내용과 개인 사진 자료는 이 기록에 포함하지 않는다.

## 로컬 실제 브라우저 변환 확인

메인이 내장 브라우저의 로컬 fixture에서 실제 canvas 변환을 확인했다. 불투명 PNG는 309,108바이트에서 JPEG 50,544바이트로, JPEG는 119,170바이트에서 46,734바이트로 줄었다. EXIF 방향 6 입력은 256×384로 올바르게 향했고 실제 투명 PNG는 원형, 투명 WebP·AVIF는 투명 PNG로 유지됐다. GIF·APNG·애니메이션 WebP는 원형을 유지했다.

24MP(6000×4000) 입력은 4898×3265로 줄어 16MP 이하를 충족했고 JPEG 115,722바이트가 됐다. 깨진 입력에는 명확한 오류가 표시됐다. 이는 합성 패턴의 기능 시험이며 실제 사진이 평균 1MB로 줄어든다는 근거로 사용하지 않는다. 관련 회귀와 최종 소스 빌드는 이 기록 시점에 코드 담당이 진행 중이다.

## 기술 설명 글 준비

글 담당이 약 2,700자 원고와 1280×940 PNG 도식을 Git 제외 작성 자료 폴더에 준비했다. 당시 메인은 실제 관리자에서 새 미공개 초안의 제목·분류를 준비했고 본문·도식 저장은 미완료였다. 이후 실제 저장 결과는 아래에 구분했다. 개인 초안의 실제 내용은 공식 문서에 옮기지 않는다.

## 최종 회귀·소스 빌드

신규 업로드·붙여넣기·UI 상태·작업본 관련 회귀와 최종 `cloudflare:build:source`를 통과했다(exit 0, Astro 오류 0). source bundle 공개 경로 1,425개와 기존 공개 요청 표면 해시는 유지됐고 비공개 자료 누출은 0이었다. 코드 담당 결과와 메인의 최종 로그 확인을 반영한 값이다. 앞선 빌드 대기 기록은 당시 진행 상태이며 현재는 통과했다. 이 빌드 확인 당시에는 운영 배포·운영 읽기와 실제 미공개 초안의 본문·도식 저장을 기다리고 있었다.

## 원격 푸시와 당시 자동 배포 접수 지연

기능 `0963121`의 main/origin 푸시와 GitHub 실제 커밋 존재를 확인했다. 2026-09-23 02:08 KST까지 Cloudflare 공개·관리자 양쪽에 이 소스의 새 빌드가 나타나지 않았고 이전 `46f49b3` 성공만 표시됐다. 운영 관리자는 기존 업로드 문구를 보여 새 기능은 아직 반영되지 않았다. 연결 저장소·main 브랜치·watch 경로 `*`를 읽기 확인했으며 권한이나 설정은 변경하지 않았다. 이후 실제 진행 문서의 커밋·푸시로 새 Git 이벤트를 전달했고 아래와 같이 배포가 접수·완료됐다. 당시에는 요구사항을 완료 처리하지 않았다.

## 실제 미공개 기술 글 저장

요청한 기술 글은 CMS에 제목·약 2,700자 본문·태그·요약·분류와 PNG 도식 1개(1280×940, 98,726바이트)를 미공개로 저장했다. PC 미리보기에서 본문·도식·가운데 캡션을 확인했다. 개인 초안 ID와 실제 본문은 공식 문서나 Git에 기록하지 않는다.

도식 첨부 시점에는 기능 배포가 대기 중이어서 기존 업로드 경로를 사용했다. 이 저장을 신규 압축 기능의 운영 실증으로 간주하지 않는다. 글은 공개 발행하지 않았다.

## 최종 배포·운영 반영과 완료 판정

문서 후속 `1846ab5`의 Git 푸시로 Cloudflare 빌드가 접수됐다. 2026-09-23 02:16 KST에 공개 recent builds Success와 관리자 build history의 같은 커밋 성공 표시를 확인했다. 관리자 빌드는 약 02:15:20 KST에 종료됐으며 소요 시간은 5분 26초였다. 기능 소스는 `0963121`, 이를 포함해 실제 배포된 커밋은 `1846ab5`로 구분한다.

메인이 자체 관리자 탭을 전체 새로고침하고 작성한 초안을 다시 열었다. 사진 첨부 패널에 “일반 사진은 용량을 줄이고, 투명도와 움직이는 사진은 유지합니다.”라는 새 안내가 표시돼 운영 반영을 확인했다. 사용자 기존 탭·글은 건드리지 않았다. 운영 공개 홈도 새로 열어 최근 글과 탐색이 정상임을 확인했다. 관리자 첨부 패널을 닫은 뒤 마지막 저장 02:07:12 KST와 아직 방문자 미공개 상태가 그대로여서 읽기 확인 중 추가 저장·공개 변경이 없었다. 현재 목록은 새 초안 1개가 추가된 602개이며 앞선 `CORE-029`의 601글 전수조사 범위를 602개로 확대해 기록하지 않는다.

기술 글의 본문·도식·태그·요약·분류를 미공개로 저장하고 새로고침 재열기와 PC 미리보기까지 확인했다. 글을 공개하지 않았고 기존 사진의 변환·삭제도 없다. 초안의 실제 본문·ID·개인 사진 정보는 Git에 넣지 않는다.

실제 canvas 압축·형식 보존 시험은 로컬에서 수행했다. 운영에서는 새 기능 배포·화면 안내와 요청한 미공개 글 저장을 확인했으며 새 압축 경로로 실제 파일을 업로드한 시험은 하지 않았다. 기존 UI로 첨부한 글 도식을 새 압축의 운영 실증으로 간주하지 않는다. 이 확인 범위를 유지해 `DWNC-CORE-031`의 안정 ID·Acceptance·Evidence를 완료 원장으로 옮겼다.

## 후속 문구 정정(2026-09-23 09:02 KST)

사용자가 열람 시 사진을 처리하는 것으로 오해할 수 있다고 지적해, 최신 운영 미공개 작업본을 다시 열고 해당 문장만 작성 중 첨부·붙여넣기 시 업로드 전에 처리한다는 내용으로 고쳤다. 메인이 사용자 브라우저에서 한정 수정했고 09:02:40 작업본 저장 성공과 미공개 유지를 확인했다. 하위 에이전트는 Git 제외 원고 MD·HTML의 같은 문단만 동기화했다. 기능·도식·나머지 본문·공개 상태는 변경하지 않았다.
