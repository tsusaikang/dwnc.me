# 본문 서식 포함·제외 붙여넣기

- 요구사항: DWNC-CORE-024
- 사용자 결정: 일반 관례 유지. Cmd+V 서식 포함, Shift+Cmd+V 무서식. Ctrl+Shift+V와 Mac Option+Shift+Cmd+V 호환.
- 상태: 기능 소스0e3d5e2의 양쪽 운영 배포·운영 관리자 읽기 확인 완료.

## 변경과 범위

기존 일반 붙여넣기는 유지한다. 무서식 단축키 의도일 때만 HTML 원문 자동 변환과 사진 업로드를 건너뛰고 브라우저 기본 문자열 삽입을 따른다. 텍스트 없는 사진만의 무서식 붙여넣기는 삽입하지 않는다. 무서식 의도는 한 번만 소비하고 키 해제·다른 키·포커스 이탈·시간 만료·글 변경 시 초기화한다. 일반/메뉴 붙여넣기의 HTML·사진 처리와 기존 자동링크·실행 취소·저장 흐름은 유지한다.

판단은 현재 기능 코드와 이번 운영 관리자 화면을 기준으로 했다. 운영 글이나 작업본을 시험 입력·저장·공개하지 않는다. 운영 원문·브라우저 기록은 Git 제외 migration/private/core024_20260917/에 둔다. 구현·독립 검토는 하위 에이전트, 사용자 세션에 묶인 브라우저·배포·통합·공식 기록은 메인이 맡는다.

## 브라우저 검증 한계

내장 브라우저 자동화의 Meta+Shift+V 및 native super+shift+v는 실제 keydown 없이 trusted:false의 paste 이벤트와 text/plain만 전달했다. 클립보드에 text/html을 함께 넣어도 동일했다. 반면 Meta+A는 실제 keydown/keyup이 기록됐다. 이 때문에 자동화 단축키 실행만으로 제품의 실제 물리 키보드 동작을 검증할 수 없다. 임시 합성 진단으로 이를 확인했으며 진단은 제품 코드에 넣지 않는다.

기존 fixture의 합성 이벤트와 브라우저 기본 입력 대용 동작으로 두 경로를 확인하고, 실제 물리 키보드 시험과 구분한다. 도구가 단축키를 일반 paste로 치환한 결과를 제품 결함이나 무서식 성공으로 오판하지 않는다.

## 검증 결과

관련6개 회귀 통과: test-editor-paste, test-html-source-paste, test-editor-autolink, test-editor-body-history, test-editor-ui-state, test-editor-working-copy. 최종 cloudflare:build:source 통과. 별도 기존 홈 카드 개수 검사 실패는 이번 범위에서 수정하지 않았으며 전체 회귀 통과를 주장하지 않는다. 독립 코드 검토에서 차단 문제 없음.

실제 브라우저의 기존 합성 fixture를 통해 서식 포함 굵은 글씨 유지, 기본 문단의 무서식 굵기 제거, HTML 원문 무서식 시 태그 문자 유지/서식 포함 시 변환, 문단 보존, 각 붙여넣기 실행 취소와 자동저장 후 새로고침·재열기를 확인했다. 무서식은 붙여넣는 곳의 기존 스타일을 따르므로 굵은 문단에 붙이면 기존 굵기는 이어질 수 있다. 출처의 서식 제거와 현재 문단의 서식 제거는 다르다.

무서식 이미지 업로드0·HTML 변환 요청0과 단축키1회소비·키업/blur/시간 만료/글 변경 초기화는 회귀로 확인했다. 자동링크 보존은 관련 회귀와 기존 native 입력 경로 불변을 검토했다. 이번 합성 execCommand 대용 입력에서는 URL 자동링크까지 실제 브라우저 성공으로 주장하지 않는다. 임시 이벤트 진단은 제거하고 일반 합성 비교 버튼만 기존 fixture에 남겼다. 제품 UI에는 시험·진단 버튼을 넣지 않았다.

## 운영 확인과 마감

0e3d5e2 공개·관리자 Git 자동 빌드와 배포가 성공했다. 관리자01:45:13 KST git-delivery-complete를 확인했다. 새 인증 관리자 화면에서598편 목록 로드와 무서식 의도 처리 코드 반영, 합성 시험 UI가 제품에 없는 것을 확인했다. 사용자 기존 편집 탭은 새로고침하지 않고 보존했으며, 운영 글·작업본에 입력·저장·공개하지 않았다. 실제 물리 키보드 확인은 위 도구 제약으로 미실행이며 코드 회귀·합성 동작과 구분한다.

완료 원장으로 옮기고 관련 코드·시험·상태·기록을 GitHub에 동기화한다. 마감 문서 전용 푸시가 다시 자동 빌드를 시작해도 기능 소스는 위와 동일하다.
